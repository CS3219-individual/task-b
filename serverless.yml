# serverless.yml

service: my-express-application

provider:
  name: aws
  runtime: nodejs12.x
  stage: dev
  region: ap-southeast-1
  environment:
    MONGO_URI: '${MONGO_URI}'

functions:
  app:
    handler: src/lambda.handler
    events:
      - http: ANY /
      - http: 'ANY {proxy+}'