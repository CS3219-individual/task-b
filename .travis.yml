language: node_js
services:
  - docker
  - mongodb

env:
  - ENVIRONMENT=TESTING

before_install:
  - docker-compose -f docker-compose.ci_test.yml build

script:
  - npm install
  - npm run postinstall

after_script:
  - bash -c "./wait-for-it.sh localhost:27017 && npm test"